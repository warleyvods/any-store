
spring:
  application:
    name: fbills

  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"
    default-property-inclusion: non_null
    deserialization:
      fail-on-null-for-primitives: true

  profiles:
    active: ${SCOPE:stage}

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

api:
  app:
    jwtCookieName: token
    jwtSecret: ${SECRET:localSecret}
    jwtExpirationMs: 86400000


cloud:
  aws:
    credentials:
      access-key: AKIARMOR3EE6OFJ4P2PA
      secret-key: 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
    region:
      static: us-east-1
    stack:
      auto: 'false'
    bucket:
      name: anystorebucket
